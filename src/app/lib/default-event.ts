/**
 * The single event this deployment is seeded with (src/db/seed.ts). The slug
 * was previously copy-pasted into three unrelated screens, which made the
 * public CFP and schedule links drift from the organizer's own redirect.
 */
export const DEFAULT_EVENT_SLUG = 'demo-conf-2026'
export const DEFAULT_FORM_SLUG = 'cfp'
